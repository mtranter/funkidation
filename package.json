{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^4.3.6",
    "lerna": "^3.22.1"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn test"
    }
  }
}
